---
title: "Unban"
description: "Unban a user from the server."
---

## 🔁 Aliases

No Aliases

## 🛡️ Required Permissions

<Warning>
  This command requires the user to have the `BanMembers` permission.
</Warning>

<Warning>
  The bot must also have the `BanMembers` permission to execute this command.
</Warning>

---

## 💡 Usage

### Basic Usage:

```plaintext
.unban <user>
````

### Parameters:

* **`<user>`**: The user you want to unban. **(Required)**
  This should be the username with discriminator or user ID.

### Example:

```plaintext
.unban User#1234
```

This will unban `User#1234` from the server.

---

## ⚠️ Notes

* The user must already be banned to be unbanned.
* Make sure to enter the exact username and discriminator or user ID to unban successfully.
* The bot must have the `BanMembers` permission to perform this action.

